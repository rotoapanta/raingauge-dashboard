/**
 * config.ts
 *
 * Global configuration for the frontend application. Defines the base URL for backend API requests.
 *
 * Configuración global para la aplicación frontend. Define la URL base para las peticiones a la API del backend.
 */

// Base URL for backend API / URL base para la API del backend
export const RPI_BASE_URL = import.meta.env.VITE_BACKEND_URL;
